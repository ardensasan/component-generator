const {{camelCase name}}Reducer = (
  state = { {{camelCase name}}List: [], defaultFieldValues: {}, dialog: false },
  action: any
) => {
  switch (action.type) {
    case "GET_{{constantCase name}}_LIST":
      return { ...state, {{camelCase name}}List: action.payload, defaultFieldValues: {}, dialog:false };
    case "GET_{{constantCase name}}_DETAILS":
      return { ...state, defaultFieldValues: action.payload, dialog: true };
      
    case "OPEN_DIALOG":
      return { ...state, defaultFieldValues: {}, dialog: true };
    case "CLOSE_DIALOG":
      return { ...state, dialog: false, defaultFieldValues: {} };
    default:
      return state;
  }
};

export default {{camelCase name}}Reducer;
