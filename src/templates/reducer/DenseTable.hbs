const {{camelCase name}}Reducer = (state={ {{camelCase name}}List:[]},action:any) => {
    switch(action.type){
        case "GET_{{constantCase name}}_LIST": 
        return {...state,{{camelCase name}}List: action.payload}
        default:
            return state;
    }
}

export default {{camelCase name}}Reducer 