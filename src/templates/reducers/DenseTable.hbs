const {{camelCase name}}Reducer = (state={},action:any) => {
    switch(action.type){
        case "GET_{{constantCase name}}_LIST": 
        return {...state,...action.payload}
        default:
            return state;
    }
}

export default {{camelCase name}}Reducer 